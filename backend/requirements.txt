# Backend inference deps (slim, for Docker / Cloud Run).
# NOTE: torch + torchvision are installed separately as CPU-only wheels
# in the Dockerfile. Do NOT list them here, and do NOT add training-only
# packages (mlflow, jupyter, seaborn, etc.).
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
pillow==12.2.0
numpy==2.4.6
